# Error Messages Decoded

> TODO: Write this section.

## Overview

Alloy error messages are often unhelpful or misleading. This page translates common errors into what they actually mean.

## Error Reference

### "component does not exist or is out of scope"

Usually means: typo in a component reference, or referencing a component from inside a module that can't see it.

### Parse errors from trailing commas

### Type mismatch errors

### Module scope errors

## Debugging Tips

- Use the Web UI at `http://localhost:12345` to visualize the pipeline
- Check component health status in the UI
- Use `alloy fmt` to catch syntax issues before running

## Summary
